#include "BaseCommand.h"

// No specific implementation needed for BaseCommand.cpp beyond what's in the header
// as its primary role is to inherit from CLI::Command and provide the CloudAPIClient instance.